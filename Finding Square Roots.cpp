#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    int t,no;
    cin>>t;
    while(t>0)
    {
        cin>>no;
        cout<<(int)sqrt(no)<<endl;
        t--;
    }
    return 0;
}
